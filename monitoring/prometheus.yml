global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "faq-api"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["faq-api:8080"]
        labels:
          application: faq-api
